# Dogfood example: Drift Protocol attack cover

Generated by `blog-cover-skill` v0.1 as the first end-to-end dogfood run.

## What this demonstrates
- 3-concept exploration produced 3 genuinely distinct metaphors
  (time-split / recon-chain / artifact-as-evidence)
- User picked concept C (artifact-as-evidence: mock multisig governance UI)
- Review subagent rated 4/10 initially due to a CSS path bug
  (promoting from `.concepts/` to `.blog-covers/` broke `../_shared.css`)
- After fixing the path rewrite + adding the fix to SKILL.md Step 7,
  the cover renders correctly

## To regenerate
```bash
# In any repo with DESIGN.md/BRAND.md
/blog-cover blog-content/07-drift-protocol-attack-lessons.md
```
